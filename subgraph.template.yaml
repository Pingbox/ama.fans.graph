specVersion: 0.0.2
schema:
  file: ./schema.graphql
dataSources:
  - kind: ethereum
    name: MessageFacet
    network: '{{network}}'
    source:
      address: '{{mainAddress}}'
      abi: MessageFacet   
      startBlock: {{startBlock}}

    mapping:
      kind: ethereum/events
      apiVersion: 0.0.5
      language: wasm/assemblyscript
      entities:
        - MessageCreated
        - MessageValueClaimed
        - ResponseCreated
        - TipCreated
        - TipValueClaimed
        - ResponseMarked
        - AmountReceived
        - DonationMade
        
      abis:
        - name: MessageFacet
          file: ./abis/MessageFacet.json
      eventHandlers:

        - event: MessageCreated(indexed address,indexed address,indexed bytes32,string,uint256,uint256)
          handler: handleMessageCreated

        - event: MessageValueClaimed(indexed bytes32,indexed address,uint256)
          handler: handleMessageValueClaimed


        - event:  ResponseCreated(indexed address,indexed address,indexed bytes32,string,uint256,uint256)
          handler: handleResponseCreated
        
        - event: TipCreated(indexed bytes32,indexed bytes32,indexed address,uint256)
          handler: handleTipCreated

        - event: TipValueClaimed(indexed bytes32,indexed bytes32,indexed address,uint256)
          handler: handleTipValueClaimed

        - event: ResponseMarked(indexed bytes32,indexed address,indexed address,uint256)
          handler: handleResponseMarked
        
        - event: AmountReceived(indexed address,uint256)
          handler: handleAmountReceived

        - event: DonationMade(address,address,uint256)
          handler: handleDonationMade


      file: ./src/mappingMessage.ts


  - kind: ethereum
    name: UserFacet
    network: '{{network}}'
    source:
      address: '{{mainAddress}}'
      abi: UserFacet   
      startBlock: {{startBlock}}

    mapping:
      kind: ethereum/events
      apiVersion: 0.0.5
      language: wasm/assemblyscript
      entities:
        - Withdraw
        - Blocked
        - UnBlock
        - Whitelisted
        - UnWhitelisted
        - Follow
        - UnFollow
      abis:
        - name: UserFacet
          file: ./abis/UserFacet.json
      eventHandlers:


        - event: Blocked(indexed address,indexed address)
          handler: handleBlocked

        - event: UnBlock(indexed address,indexed address)
          handler: handleUnBlock

        - event: Whitelisted(indexed address,indexed address)
          handler: handleWhitelisted
        
        - event: UnWhitelisted(indexed address,indexed address)
          handler: handleUnWhitelisted

        - event: Follow(indexed address,indexed address)
          handler: handleFollow
        
        - event: UnFollow(indexed address,indexed address)
          handler: handleUnFollow

        - event: RegisterFiatUser(indexed address)
          handler: handleRegisterFiatUser

        - event: UserValidation(address,address,string)
          handler: handleUserValidation
    
        - event: DonationAddressChanged(address,address)
          handler: handleDonationAddressChanged

      file: ./src/mappingUser.ts


  - kind: ethereum
    name: OwnershipFacet
    network: '{{network}}'
    source:
      address: '{{mainAddress}}'
      abi: OwnershipFacet   
      startBlock: {{startBlock}}

    mapping:
      kind: ethereum/events
      apiVersion: 0.0.5
      language: wasm/assemblyscript
      entities:
        - RoleAdminChanged
        - RoleGranted
        - RoleRevoked
        - OwnershipTransferred
      abis:
        - name: OwnershipFacet
          file: ./abis/OwnershipFacet.json
      eventHandlers:
        
        - event: RoleAdminChanged(indexed bytes32,indexed bytes32,indexed bytes32)
          handler: handleRoleAdminChanged

        - event: RoleGranted(indexed bytes32,indexed address,indexed address)
          handler: handleRoleGranted

        - event: RoleRevoked(indexed bytes32,indexed address,indexed address)
          handler: handleRoleRevoked

        - event: OwnershipTransferred(indexed address,indexed address)
          handler: handleOwnershipTransferred

      file: ./src/mappingOwnership.ts

  - kind: ethereum
    name: CommonFacet
    network: '{{network}}'
    source:
      address: '{{mainAddress}}'
      abi: CommonFacet   
      startBlock: {{startBlock}}

    mapping:
      kind: ethereum/events
      apiVersion: 0.0.5
      language: wasm/assemblyscript
      entities:
        - Transfer
        - withdraw
        - FiatUserWithdraw
        - AdminWithdrawForRole
        - AmountReceived

      abis:
        - name: CommonFacet
          file: ./abis/CommonFacet.json
      eventHandlers:
        - event: Transfer(indexed address,indexed address,uint256)
          handler: handleTransfer

        - event: Withdraw(indexed address,uint256)
          handler: handleWithdraw

        - event: FiatUserWithdraw(indexed address,uint256)
          handler: handleFiatUserWithdraw

        - event: AdminWithdrawForRole(indexed address,indexed address,indexed address,bytes32,uint256)
          handler: handleRoleWithdraw
    

      file: ./src/mappingCommon.ts

  # - kind: ethereum
  #   name: DiamondCutFacet
  #   network: '{{network}}'
  #   source:
  #     address: '{{diamondCutFacetaddress}}'
  #     abi: DiamondCutFacet   
  #     startBlock: {{startBlock}}

  #   mapping:
  #     kind: ethereum/events
  #     apiVersion: 0.0.5
  #     language: wasm/assemblyscript
  #     entities:
  #       - DiamondCut
  #     abis:
  #       - name: DiamondCutFacet
  #         file: ./abis/DiamondCutFacet.json
  #     eventHandlers:
  #       - event: DiamondCut((address,uint8,bytes4[])[],address,bytes)
  #         handler: handleDiamondCut

  #     file: ./src/mappingDiamondCut.ts